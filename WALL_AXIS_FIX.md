# CENTERLINES — исправление горизонтального смещения стен в IFC

Версия: v6 + wall_axis_fix
Изменён **один** файл продукта: `ifc_writer.py`.
Ни DXF, ни `centerlines.py`, ни `agent.py`, ни `building_builder.py`,
ни `building_setup.py`, ни `unified_sdnf_writer.py`, ни SDNF **не менялись**.

---

## 1. Трассировка координат одной стены (до исправления)

Тестовая стена: `A = (0.000, 0.000)`, `B = (3.000, 0.000)`, `T = 0.200`, `L = 3.000`.

| Этап | Что получено | Верно? |
|---|---|---|
| DXF (`walls_200`, LINE) | start (0,0) → end (3000,0) | да |
| `agent.read_walls_from_osi()` | `x1=0, y1=0, x2=3.0, y2=0` (×scale, мм→м) | да |
| Floor Processor / `run_floor_processor()` | только добавляет `z1`, `z2` | да |
| объект стены CENTERLINES | `{x1:0, y1:0, x2:3.0, y2:0, thickness:0.2}` | да |
| `IFCLOCALPLACEMENT` → `IFCAXIS2PLACEMENT3D` | origin `(0,0,0)`, RefDirection `(1,0,0)` | да |
| `IFCSHAPEREPRESENTATION 'Axis'` | полилиния `(0,0) → (3.0,0)` | да |
| `IFCRECTANGLEPROFILEDEF` (Body) | Position `(0,0)`, XDim `3.0`, YDim `0.2` | **НЕТ** |
| Итог в Forum (мировые координаты тела) | **X от −1.500 до +1.500** вместо 0…3.000 | **НЕТ** |

Смещение возникает **ровно на одном шаге** — в профиле тела стены.

---

## 2. Причина

`IfcRectangleProfileDef` по стандарту IFC **всегда центрируется относительно
собственного `Position`**: прямоугольник занимает

```
X ∈ [−XDim/2 … +XDim/2]
Y ∈ [−YDim/2 … +YDim/2]
```

В коде профиль стены строился через `profile_rect()` с `Position = (0,0)`:

```python
prof = self.profile_rect("SCADPARAMPROF_WALL", L, t)   # Position = (0,0)
```

А `IfcLocalPlacement` стены ставился в точку **A** с локальной осью X,
направленной от A к B. В результате тело стены раскладывалось как

```
от  A − (L/2)·û   до   A + (L/2)·û
```

то есть **стена строилась вокруг середины отрезка A–B**, а не от A до B.
Каждая стена «съезжала» назад по своей оси ровно на `L/2`.

Именно это превращает Г-образный узел в крест: горизонтальная стена
3000 мм уезжала на 1500 мм назад, вертикальная 4000 мм — на 2000 мм вниз,
и обе начинали торчать по обе стороны от узла.

Косвенное подтверждение было прямо в файле: представление `'Axis'`
(полилиния `0,0 → L,0`) уже было правильным и **противоречило** телу
`'Body'`. Функция `profile_wall()` в файле существовала, имела комментарий
про «начало от точки оси», но фактически **не вызывалась** и содержала ту
же ошибку (`Position = (0,0)`).

Дополнительно найдена вторая, сопутствующая ошибка:

```python
IFCMATERIALLAYERSETUSAGE(#ls, .AXIS1., .POSITIVE., -T/2, $)
```

`.AXIS1.` для стены означает «вдоль длины стены». Импортёры, которые
используют `MaterialLayerSetUsage` для позиционирования слоёв, получали
указание сдвинуть слой на `−T/2` **вдоль оси стены**. Для стены
направление набора слоёв обязано быть `.AXIS2.` (поперёк стены).

---

## 3. Что изменено (файл `ifc_writer.py`)

### 3.1 `IFCWriter.profile_wall(length, thickness)` — строки ~24-45

```python
-        pos = self.a2(self.p2(0,0),self.d2(1,0))
+        pos = self.a2(self.p2(length/2.0,0.0),self.d2(1,0))
```

Профиль сдвигается на `+L/2` **только вдоль локальной оси X**. Тогда:

* локальный X: `0 … L` — длина начинается точно в A и заканчивается в B;
* локальный Y: `−T/2 … +T/2` — толщина остаётся симметричной относительно оси.

Поперёк оси никакого смещения не добавлено. Порядок точек A→B не меняется.

### 3.2 `IFCWriter.wall()` — строка ~97

```python
-        prof=self.profile_rect("SCADPARAMPROF_WALL",L,t)
+        prof=self.profile_wall(L,t)
```

Стена перестала использовать `profile_rect()` (он центрирует и длину тоже).
`profile_rect()` оставлен без изменений и по-прежнему используется
**колоннами** — для колонны центрирование по обеим осям правильно.

### 3.3 `IFCWriter.wall()` — материал, строки ~110-117

```python
-        layer=self.add(f"IFCMATERIALLAYER($,{t:.6f},.U.,$,$,$,$)")
+        layer=self.add(f"IFCMATERIALLAYER({self.R(self.material)},{t:.6f},.U.,$,$,$,$)")
...
-        use=self.add(f"IFCMATERIALLAYERSETUSAGE({self.R(ls)},.AXIS1.,.POSITIVE.,{-t/2:.6f},$ )")
+        use=self.add(f"IFCMATERIALLAYERSETUSAGE({self.R(ls)},.AXIS2.,.POSITIVE.,{-t/2:.6f},$)")
```

`.AXIS1.` → `.AXIS2.` (набор слоёв поперёк стены).
`OffsetFromReferenceLine = −T/2` при `.POSITIVE.` даёт слой `−T/2 … +T/2`,
то есть толщина симметрична относительно осевой линии.

### 3.4 `IFCWriter.init()` — строка ~73

```python
+        self.material=self.add("IFCMATERIAL('Concrete')")
```

Один общий `IfcMaterial` для `IfcMaterialLayer` (раньше был `$`).

**Итого: 4 правки, все внутри `ifc_writer.py`, все относятся только к стенам.**

---

## 4. Что НЕ менялось

`centerlines.py`, `agent.py`, `building_builder.py`, `building_setup.py`,
`plate.py`, `unified_sdnf_writer.py`, `building.json`, все `*.dxf`,
слои `walls`/`columns`/`slabs`, правила LINE/LWPOLYLINE, Node Registry,
многоэтажность, высоты этажей, плиты, отверстия, фундаментная плита,
колонны, SDNF, типы `IfcColumn` / `IfcWallStandardCase` / `IfcSlab`.

Структура IFC-стены осталась прежней (как у Forum):
`IfcLocalPlacement` → `IfcAxis2Placement3D` → `IfcShapeRepresentation`
(`Axis` + `Body`) → `IfcExtrudedAreaSolid` → `IfcRectangleProfileDef`,
плюс `IfcMaterialLayer` / `IfcMaterialLayerSet` / `IfcMaterialLayerSetUsage`.
Новых сущностей не вводилось, кроме одного `IfcMaterial`.

---

## 5. Тесты

### 5.1 `test_wall_axis.py` — эталонный Г-образный тест (раздел 11 ТЗ)

Строит IFC настоящим `IFCWriter`, затем **читает его обратно**, разбирает
цепочку `IfcLocalPlacement → IfcAxis2Placement3D → IfcExtrudedAreaSolid →
IfcRectangleProfileDef` и восстанавливает мировые координаты углов тела.

Стены теста:

| # | A | B | L | описание |
|---|---|---|---|---|
| 1 | (0, 0) | (3.0, 0) | 3000 мм | горизонтальная |
| 2 | (3.0, 0) | (3.0, 4.0) | 4000 мм | вертикальная (Г-узел с №1) |
| 3 | (3.0, 4.0) | (6.0, 7.0) | 4242.6 мм | наклонная 45° |
| 4 | (6.0, 7.0) | (6.0, 2.0) | 5000 мм | обратное направление (Y1 > Y2) |

T = 200 мм для всех.

Проверки для каждой стены:
* тело начинается в A (`s_min = 0`);
* тело заканчивается в B (`s_max = L`);
* длина тела = `distance(A,B)`;
* толщина симметрична: `−T/2 … +T/2`;
* origin `IfcLocalPlacement` == A;
* представление `Axis` идёт A → B;
* узловая проверка: конец оси стены 1 == начало оси стены 2 (Г остаётся Г).

**До исправления:** 8 FAIL — все стены смещены на `−L/2`
(например, стена 1: тело `−1.500 … +1.500` вместо `0.000 … 3.000`).

**После исправления:** `RESULT: ALL CHECKS PASSED`.

Запуск:
```
python test_wall_axis.py
```

### 5.2 `verify_ifc_walls.py` — проверка всего здания

Прогоняет ту же геометрическую проверку по **всем** стенам реального
`building.ifc`.

Полный прогон `python building_builder.py building.json` на исходных
`lvl 0/1/2/5.dxf` (6 этажей):

```
Этаж 0: стены=65,  колонны=51, плиты=1, отверстия=0
Этаж 1: стены=124, колонны=31, плиты=3, отверстия=10
Этаж 2: стены=161, колонны=0,  плиты=1, отверстия=12
Этаж 3: стены=161, колонны=0,  плиты=1, отверстия=12
Этаж 4: стены=161, колонны=0,  плиты=1, отверстия=12
Этаж 5: стены=161, колонны=0,  плиты=1, отверстия=12
Всего стен: 833, колонн: 82, плит: 8, отверстий: 58
Глобальных узлов: 1761, элементов: 981
```

Счётчики совпадают с v6 — конвейер не затронут.

Результат проверки IFC:

```
IFCBUILDINGSTOREY        6
IFCCOLUMN                82
IFCWALLSTANDARDCASE      833
IFCSLAB                  8

walls checked        : 833
walls with offset    : 0
max |start offset|   : 0.000000500 m   (0.0005 мм — округление %.6f в STEP)
max |length error|   : 0.000000000 m
RESULT: OK - no wall offset
```

Запуск:
```
python verify_ifc_walls.py building.ifc
```

### 5.3 Иллюстрация

`wall_axis_fix_before_after.png` — план Г-узла 3000+4000 мм, T=200 мм,
слева «до» (крест), справа «после» (Г).

---

## 6. Критерий готовности

| Требование | Статус |
|---|---|
| стены совпадают с исходными осями DXF | OK (833/833) |
| длина стены = distance(A,B) | OK (макс. ошибка 0.0 м) |
| Г-образные стены остаются Г-образными | OK |
| нет превращения Г в крест | OK |
| горизонтальные стены не смещены | OK |
| вертикальные стены не смещены | OK |
| наклонные стены не смещены | OK (тест 45°) |
| порядок точек A→B сохранён | OK (тест стены №4, Y1 > Y2) |
| толщина симметрична относительно оси | OK (−T/2 … +T/2) |
| этажи сохраняют правильные Z | OK (6 этажей, Z −2.2 … 13.6) |
| колонны и плиты продолжают работать | OK (82 IfcColumn, 8 IfcSlab) |
| SDNF не изменён | OK |
